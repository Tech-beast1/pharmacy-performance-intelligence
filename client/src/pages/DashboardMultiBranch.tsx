import Dashboard from './Dashboard';

// For now, organization owners see the same dashboard as single pharmacy owners
// The multi-branch analytics will be added in a future phase
export default function DashboardMultiBranch() {
  return <Dashboard />;
}
